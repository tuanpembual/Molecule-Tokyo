---
dependency:
  name: galaxy
ansible:
  group_vars:
    development:
      env_name: Development
driver:
  name: docker
docker:
  containers:
    - name: nginxDocker
      image: tuanpembual/leap-systemd
      image_version: latest
      privileged: True
      command: /bin/bash
      ansible_groups:
        - development
verifier:
  name: serverspec
